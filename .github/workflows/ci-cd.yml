name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  JAVA_VERSION: '21'
  DOCKER_IMAGE: grab-store

jobs:
  # ===========================================
  # JOB 1: Build and Test
  # ===========================================
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: maven

      - name: Grant execute permission for Maven wrapper
        run: chmod +x ./mvnw

      - name: Build with Maven
        run: ./mvnw clean package -DskipTests -B

      - name: Run tests
        run: ./mvnw test -B

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-jar
          path: store/target/*.jar
          retention-days: 1

  # ===========================================
  # JOB 2: Build and Push Docker Image
  # ===========================================
  docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===========================================
  # JOB 3: Deploy (Optional - uncomment if needed)
  # ===========================================
  # deploy:
  #   name: Deploy to Server
  #   runs-on: ubuntu-latest
  #   needs: docker
  #   if: github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - name: Deploy to server via SSH
  #       uses: appleboy/ssh-action@v1.0.0
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         script: |
  #           docker pull ${{ secrets.DOCKER_USERNAME }}/grab-store:latest
  #           docker stop grab-store || true
  #           docker rm grab-store || true
  #           docker run -d --name grab-store \
  #             -p 8080:8080 \
  #             -e SPRING_PROFILES_ACTIVE=default \
  #             -e PRODUCT_DATASOURCE_URL=${{ secrets.DB_URL }} \
  #             -e PRODUCT_DATASOURCE_USERNAME=${{ secrets.DB_USER }} \
  #             -e PRODUCT_DATASOURCE_PASSWORD=${{ secrets.DB_PASSWORD }} \
  #             ${{ secrets.DOCKER_USERNAME }}/grab-store:latest
